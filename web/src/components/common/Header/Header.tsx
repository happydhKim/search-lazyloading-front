import * as React from 'react';
import S from './style';

const Header: React.FC = () => {
  const a = 3;

  return (
    <S.HeaderWrapper>
      Header
    </S.HeaderWrapper>
  );
};

export default Header;
